import "./useTitleCount";
import useTitleCount from "./useTitleCount";

const Titleusing = ({ coutn }) => {
  useTitleCount(coutn);

  return <h1>count value is coutn</h1>;
};

export default Titleusing;
